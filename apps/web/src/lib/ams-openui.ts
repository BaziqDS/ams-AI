"use client";

import { openuiLibrary } from "@openuidev/react-ui";

// Central OpenUI contract for the AMS frontend. This intentionally uses
// OpenUI's built-in Zod-validated component library only.
export const amsOpenUiLibrary = openuiLibrary;
